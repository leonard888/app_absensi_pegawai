import 'package:flutter/material.dart';

class EmployeeInputPage extends StatelessWidget {
  const EmployeeInputPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
